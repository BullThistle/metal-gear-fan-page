<%= render "layouts/errors", :object => @gear %>

<%= form_for @gear do |f| %>
  <%= f.label :name %>
  <div class="row">
    <div class="large-6 columns">
      <%= f.text_field :name %>
    </div>
  </div>
  <%= f.submit :class => 'button' %>
<% end %>
