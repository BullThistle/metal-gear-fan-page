<%= image_tag "logo.png" %>
<h1>games</h1>

<ul>
  <% @games.each do |game| %>
    <li><%= link_to game.name, game_path(game) %></li>
  <% end %>
</ul>

<hr>

<%= link_to "New game", new_game_path %>
